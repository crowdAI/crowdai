$("#load-more").remove();
$("ul.list-tutorials").append("<%= j render partial: 'articles' %>");
$("ul.list-tutorials").after("<%= j render partial: 'shared/load_more', locals: { collection: @articles } %>");
$('html, body').animate({
   scrollTop: $(document).height()-$(window).height()},
   1400,
   "easeOutQuint"
);
