<div class="button-group">
  <%= f.button (f.object.new_record? ? "CREATE CHALLENGE" : "UPDATE CHALLENGE"), class: 'btn btn-primary' %>
  <%= button_to challenges_path, class: "btn btn-secondary" do %>
    CANCEL
  <% end %>
</div>
