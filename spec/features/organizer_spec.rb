require "rails_helper"

RSpec.feature "organizer", type: :feature do

  scenario "participant can create a organizer as draft" do
    skip("spec to be coded")
  end

  scenario "admin can approve organizers in draft" do
    skip("spec to be coded")
  end

end
