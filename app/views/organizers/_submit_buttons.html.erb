<div class="button-group">
  <%= f.submit (f.object.new_record? ? "Create Host" : "Update Host"), class: 'btn btn-primary' %>
  <%= link_to 'Cancel', challenges_path, class: "btn btn-secondary" %>
</div>
