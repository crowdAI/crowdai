<p id="notice"><%= notice %></p>

<h1>Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Participant</th>
      <th>Title</th>
      <th>Body</th>
      <th>Published</th>
      <th>Vote count</th>
      <th>View count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.participant %></td>
        <td><%= blog.title %></td>
        <td><%= blog.body %></td>
        <td><%= blog.published %></td>
        <td><%= blog.vote_count %></td>
        <td><%= blog.view_count %></td>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
        <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
