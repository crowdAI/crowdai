<%= simple_form_for([@competition,@dataset]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :competition %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
