<%= render partial: 'challenges/sidebar' %>
<%= render partial: 'challenges/header', challenge: @challenge %>

<table class='table table-hover table-striped table-bordered'>
  <thead>
    <tr>
      <th>Ranking</th>
      <th>Participant</th>
      <th>Mean F1</th>
      <th>Mean Log Loss</th>
      <th>Entries</th>
      <th>Submitted</th>
    </tr>
  </thead>

  <tbody>
    <% @leaderboards.each_with_index do |leader,row| %>
      <tr>
        <td><%= row + 1 %></td>
        <td><%= leader.participant.name %></td>
        <td><%= leader.score %></td>
        <td><%= leader.score_secondary %></td>
        <td><%= leader.entries %></td>
        <td><%= leader.updated_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
