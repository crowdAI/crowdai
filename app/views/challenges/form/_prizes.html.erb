<div class="row">
  <%= form_for @challenge, validate: true, html: { class: "dbl-col" } do |f| %>
  <fieldset>
    <label>Prizes</label>
    <%= markdown_editor("prizes_markdown") do |m| %>
      <%= f.text_area :prizes_markdown, class: "txt-med" %>
    <% end %>
    <%= render partial: 'challenges/form/submit_buttons', locals: { f: f } %>
  </fieldset>
  <% end %>
</div>
